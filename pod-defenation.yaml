apiVersion: v1
kind : Pod
metadata:
  name: myapp-pod
  labels:
     app: myapp
     type: front-end
sec:
 containers:
   - name: niginx-container
     image: nginx

# after creation of the pod definition file  we need run the file by using bellow commands.
# "kubectl create  -f pod-defenation.yml" (Here pod-defenation  is the file name)

